"use client";import Link from "next/link";import {usePathname} from "next/navigation";import {useState} from "react";import {Home,Joystick,LogIn,Medal,UserRound,Volume2,VolumeX} from "lucide-react";
const links=[{href:"/",label:"Home",icon:Home},{href:"/play",label:"Play",icon:Joystick},{href:"/leaderboard",label:"Leaderboard",icon:Medal},{href:"/profile",label:"Profile",icon:UserRound}];
export function Navigation(){const path=usePathname();const[sound,setSound]=useState(false);return <><header className="top-nav"><Link href="/" className="logo"><i>⚽</i>BALLER<span>.</span></Link><nav>{links.map(l=><Link key={l.href} href={l.href} className={path===l.href?"active":""}>{l.label}</Link>)}</nav><Link className="account-link" href="/account"><LogIn/>ACCOUNT</Link><button type="button" className="sound" onClick={()=>setSound(v=>!v)} aria-pressed={sound} aria-label={sound?"Turn sound off":"Turn sound on"} title={sound?"Sound on":"Sound off"}>{sound?<Volume2/>:<VolumeX/>}</button></header><nav className="bottom-nav">{links.map(l=><Link key={l.href} href={l.href} className={path===l.href?"active":""}><l.icon/><span>{l.label}</span></Link>)}</nav></>}
