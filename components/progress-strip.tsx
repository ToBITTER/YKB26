"use client";import {Flame,Zap} from "lucide-react";import {useProgress} from "./progress-provider";import {getLevelProgress,getLevelTitle} from "@/lib/game";
export function ProgressStrip(){const {progress}=useProgress();const level=getLevelProgress(progress.xp);return <section className="progress-strip"><div className="level-shield"><span>LVL</span><strong>{level.level}</strong></div><div className="level-copy"><p>{getLevelTitle(level.level)}</p><div className="xp-bar"><i style={{width:`${level.percent}%`}}/></div><small>{level.current.toLocaleString()} / {level.needed.toLocaleString()} XP</small></div><div className="streak"><Flame/><strong>{progress.dailyStreak}</strong><span>day streak</span></div><div className="total-xp"><Zap/><strong>{progress.xp.toLocaleString()}</strong><span>total xp</span></div></section>}

