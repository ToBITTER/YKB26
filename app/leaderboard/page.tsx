"use client";import {useState} from "react";import {Medal} from "lucide-react";import {useProgress} from "@/components/progress-provider";import {getLevelFromXP} from "@/lib/game";
export default function Leaderboard(){const{progress}=useProgress();const[tab,setTab]=useState("GLOBAL");return <main className="shell page"><header className="page-head"><span>THE TABLE</span><h1>LEADERBOARD.</h1><p>Only real profiles saved on this device are shown. No invented competitors.</p></header><div className="tabs">{["GLOBAL","WEEKLY","COUNTRY"].map(t=><button className={tab===t?"active":""} onClick={()=>setTab(t)} key={t}>{t}</button>)}</div><section className="leaderboard"><div className="leader-head"><span>RANK</span><span>PLAYER</span><span>LEVEL</span><span>XP</span></div><article className="you"><strong>#1</strong><div className="avatar">{progress.username.slice(0,2).toUpperCase()}</div><div><b>{progress.username}</b><small>YOU · {progress.country}</small></div><span>LVL {getLevelFromXP(progress.xp)}</span><strong>{progress.xp.toLocaleString()} XP</strong></article><div className="honest-empty"><Medal/><strong>THE TABLE IS YOURS—FOR NOW.</strong><p>Create more profiles on a persistent backend to compete across devices. Demo users are not presented as real players.</p></div></section></main>}

